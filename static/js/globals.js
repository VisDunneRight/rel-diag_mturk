var tutorial_page_num = 1;          // Current page number of the tutorial
var total_num_tutorial_pages = 8;   // Total number of tutorial pages

var question_num = 1;
var checking_answer = true;
const total_num_questions = 32
const num_question_choices = 4;
const min_time_per_question = 500;    // In miliseconds
var current_mode = 1;

var worker_id;
var assignment_id;
var hit_id;